import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    primary: {
      main: "#fff",
    },
    secondary: {
      main: "#000",
    },
  },
  typography: {
    h1: { fontSize: "1.5rem" },
    h2: { fontSize: "1.25rem" },
    h3: { fontSize: "1rem" },
  },
});
